<h1>Add Attendance</h1>

<%= form_for @attend do |f| %>
  <% if @attend.errors.any? %>
    <div class="error_messages">
      <h2>Form is invalid</h2>
      <ul>
        <% @attend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :events do |e| %>
    Event<br />
    <%= e.text_field :name %><br />

    <%= e.label :date %><br />
    <%= e.text_field :date %><br />

    <%= e.fields_for :artists do |builder| %>
        Artist<br />
        <%= builder.text_field :name %><br />
    <% end %>

    <%= e.fields_for :venues do |builder| %>
        Venue<br />
        <%= builder.text_field :name %><br />
    <% end %>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>

  <div class="field">
    <%= f.label :review %><br />
    <%= f.text_area :review %>
  </div>

  <div class="actions"><%= f.submit "Add Attendance" %></div>
<% end %>